# CHANGELOG.md is generated by release-please (conventional-changelog format),
# which emits double blank lines between the version heading and each section
# and dash-style list markers — patterns that trip MD012/MD004 on every
# release PR. Linting a machine-generated file with hand-authoring rules only
# produces inline noise, so exclude it from the markdownlint scan. This mirrors
# how plugins/ (a symlink mirror) is kept out of the lint/link scans.
CHANGELOG.md
